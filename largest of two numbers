#include<stdio.h>
int main()
{
    int a,b;
    scanf("%d %d",&a,&b);
    if(a>b)
    {
        printf("first number is largest");
    }
    else if(a==b)
    {
        printf("the given numbers are equal");
    }
    else
    {
        printf("second number is largest");
    }
}
