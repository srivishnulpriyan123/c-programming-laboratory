#include<stdio.h>
int main()
{
   int a,b;
   scanf("%d %d", &a,&b);
   if(a>b)
   {
       printf("%d is maximum",a);
   }   
   else if(b>a)
   {
       printf("%d is maximum", b);
   }
   else
   {
       printf("both are %d and equal",a);
   }
}
