#include<stdio.h>

int main()

{

int n,c, flag=0;

scanf("%d",&n);

int a[n], freq[n];

addtwonumber

C

for(int j = 0 i<n;i++)

{

arithmeticoper

scanf("%d",&a[i]);

} for(int i = 0; i < n ;i++)


{

array2.c array3.c

freq[i]=a[i];

}

for( int i = 0; i < n;i++)

c = 1

for(int j = i + 1; j < n ;j++)

{ if(a[i]== a[j] * 88a[i] * 1 = 999 )
{ C++; a[j]=999; flag=1; }

} if(flag==1)

{ printf("\nThe frequency of %d is %d", freq[i],c);

}

}
