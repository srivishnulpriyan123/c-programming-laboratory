#include<stdio.h>
int main()
{
    int a,b;
    scanf("%d %d",&a,&b);
    if(a==b)
    {
        printf("the given numbers are equal");
    }
    else
    {
        printf("the given numbers are not equal");
    }
}
